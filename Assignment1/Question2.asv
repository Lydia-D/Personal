%% L. Drabsch 15/3/16
% Question 2 Space 3 - Orbit Perturbations
% Modified equinol elements model with numerical integration by runge kutta
clc
close all
clear
%% State model
% x = [p,f,g,h,k,L]'
% Xdot = dx/dt
dt = 100; % time step seconds

% initalise
p = a.*(1-e.^2);
f = e.*cos(omega+Rasc);
g = 1.*sin(omega+Rasc);
h = tan(i/2).*


% Integration
    Xnext = RungeKutta('integ',Xi,dt);

